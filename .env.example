# 백엔드 주소 (Vite 개발 서버 프록시 대상)
VITE_PROXY_TARGET=http://localhost:8080

# 브라우저·프록시 공통 API 접두사 (src/lib/api.ts 와 동일)
VITE_API_PREFIX=/api

# true 일 때만 프록시에서 /api 접두사를 제거합니다. (/api/auth 는 보통 false)
# VITE_PROXY_STRIP_PREFIX=true

# 프로덕션 빌드에서 API가 별도 도메인일 때만 설정
# VITE_API_ORIGIN=
